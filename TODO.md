# TODO

- Add a way to tweak synthesis and implementation settings (strategy,
  directives, optimization goals, etc.) through the AI project itself,
  instead of editing `tcl/synth.tcl` / `tcl/impl.tcl` by hand.
